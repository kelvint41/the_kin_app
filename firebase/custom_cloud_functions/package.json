{
  "name": "functions",
  "description": "Firebase Custom Cloud Functions",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-kin-test --config ../firebase.json \"node --test test/*.test.js\""
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "firebase-admin": "^11.8.0",
    "firebase-functions": "^4.3.1"
  },
  "private": true,
  "devDependencies": {
    "firebase-functions-test": "^3.5.0",
    "firebase-tools": "^13.35.1"
  }
}
